# Postmite Project-Name Gate

## Decision

The `Postmite` project name is approved for distribution by `sino-s` as the publisher of `https://github.com/sino-s/postmite`.

## Evidence

- The repository owner is `sino-s`.
- `src-tauri/Cargo.toml` identifies `sino-s` as the package author and the same repository as its source.
- The package is Apache-2.0 licensed.

## Boundary

This is a project-name ownership decision for this release. It does not claim that `Postmite` is a registered trademark, grant rights to unrelated distributors, or replace jurisdiction-specific trademark review.
